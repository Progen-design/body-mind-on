v7 rebrand to BMON sky/lavender palette + public /plan/[id] web view

Email v7 (lib/templates/bmon_weekly_plan_email_v7.html, lib/weeklyPlanEmailV7.js):
- Retone from vivid purple/pink/gold to brand-aligned sky #0EA5E9 + lavender
  #A78BFA + cyan #22D3EE on slate-900/800 backgrounds.
- Add "Zobrazit plan v prohlizeci" link in the email header, pointing at the
  new public web view.

Public web view (pages/plan/[id].js, components/PlanWebView.js,
styles/plan-view.module.css):
- Next.js page rendered with getServerSideProps, looks up plan by UUID and
  feeds it through a multi-column responsive layout (3-col days grid on
  desktop, 2-col tablet, 1-col mobile; 2-col profile+motto on desktop).
- Sticky brand nav, hero with stats, kcal/macros panel, habits grid, 7 day
  cards (Day 1 full internal grid, Days 2-7 compact cards), final CTA, footer.
- noindex,nofollow; CDN cached 5 min with SWR 60 min.

Wiring:
- mail.js threads planId/webViewUrl through to V7 builder; downstream callers
  (admin send-test, send-plan-again, body-metrics fallback, taskExecutors,
  unifiedPlanPipeline) pass plan.id so the email contains a working link.
- JetBrains Mono added to global font load in _document.js.

Tooling:
- scripts/rebrand-v7-colors.mjs is the one-shot retoner used to apply the new
  palette to template + renderer (kept for traceability).
